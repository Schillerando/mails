Use [this](https://premailer.dialect.ca/) site to make the HTML useable for every client.
Add
``` css
  #made-with-supabase--text {
  display: none;
  }
```
to the style tag, this removes the watermark in some clients.
